{
  "name": "html-to-storyblok",
  "version": "0.1.0",
  "description": "Safe additive-only CLI for integrating supplied HTML templates into existing Storyblok projects.",
  "type": "module",
  "bin": {
    "html-to-storyblok": "./bin/html-to-storyblok.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/html-to-storyblok.js && node --check src/cli.js && node --check src/evidence.js && node --check src/inspectors.js && node --check src/policy.js && node --check src/utils.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}

